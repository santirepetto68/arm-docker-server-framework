This directory is deprecated. Config files have moved to `games/ark-se/config/`.

The files here are kept temporarily for backward compatibility with the old
`docker-compose.yml` volume mount. They will be removed in a future release.

Edit the copies under `games/ark-se/config/` instead.
