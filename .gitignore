.env
instances/*.env
install_log.txt
temp/

# Backup archives (keep the directory)
backups/**/*.tar.gz

# Game server data (large binary files, saves — keep .gitkeep placeholders)
data/server/*
!data/server/.gitkeep
data/ark-se/*
!data/ark-se/.gitkeep
data/project-zomboid/*
!data/project-zomboid/.gitkeep

# FEX-Emu state files (auto-created by the FEX runtime image on first run)
Config.json
rootfs/
